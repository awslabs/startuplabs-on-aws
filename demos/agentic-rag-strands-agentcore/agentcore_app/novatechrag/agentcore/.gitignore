# Secrets (local environment files are never committed)
.env.local

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI Internals (includes deployed-state.json which contains account ID + ARNs)
.cli/

# Ephemeral Staging
.cache/*
